import { Box } from '@chakra-ui/react';

const Logo = ({ color }: any) => (
  <Box m='auto' my={2} mx={1.5} px={1} py={0.5} fontSize='2xl' fontWeight='800' textDecoration='none' color={color}>
    ❍ UI Kit
  </Box>
);

export default Logo;
