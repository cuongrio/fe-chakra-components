import { Box } from "@chakra-ui/react";

const Logo = () => (
    <Box
      sx={{
        fontSize: "2xl",
        fontWeight: 800,
        textDecoration: "none",
        color: "blue.500",
      }}
    >
      ❍ UI Kit
    </Box>
  );

  export default Logo;